// Token IA par défaut.
//
// ⚠️ NE PAS remplir ce fichier avec le vrai token : il est committé dans git.
// Au build de release, le workflow GitHub Actions le réécrit automatiquement
// à partir du secret de dépôt `AI_API_KEY` (voir .github/workflows/release.yml).
//
// En local, laisse-le vide : pour tester l'IA depuis un build de dev, saisis la
// clé directement dans Paramètres → Assistant IA.
export const DEFAULT_AI_API_KEY = '';
